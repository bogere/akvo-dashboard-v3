import mapSidebar from 'akvov3/utils/map-sidebar';
import { module, test } from 'qunit';

module('Unit | Utility | map-sidebar', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = mapSidebar();
    assert.ok(result);
  });
});
